let value = 15;
let base = 2;

parseInt("value", base); // converts to base 10
value.toString(base); // convert from base 10 to other bases

let myArr = [23, 4, 4, 4, 1, 23, 5, 6, 32, 1];

let newAarr = myArr.filter((el, ind, arr) => el);

console.log(newAarr);

/**let me = {
  89: 1,
  90: 2,
  91: 1,
  92: 3,
  93: 1,
  94: 2,
};
*/
